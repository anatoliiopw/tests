pipeline {
    agent {label 'AWS-BUILD'}
    stages {
        stage('Checkout all components') {
            steps {
                cleanWs()
                echo "Test message"
                // bat "Waiting 0 sec elapsed"
            }
        }
    }
}